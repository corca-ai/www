# Corca website — agent guide

Start at the documentation hub in [docs/index.md](docs/index.md); all project
docs live in `docs/*.md`, linted and formatted by awiki.

Quick start: `pnpm dev`, `pnpm build`, `pnpm deploy`. Run `pnpm check` before you
commit — it runs Biome, `astro check`, and the duplication and docs gates.

> Note: `CLAUDE.md` is a symlink to this file (`AGENTS.md`).
