{
  "name": "corca-www",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf:preview": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "astro": "astro",
    "lint": "biome check && astro check",
    "format": "biome check --write && awiki format -root docs",
    "dup": "nose query src",
    "docs:lint": "awiki lint -root docs",
    "docs:format": "awiki format -root docs",
    "check": "biome check && astro check && nose query src --mode syntax 'dup>80' --fail-on any && awiki lint -root docs",
    "prepare": "lefthook install"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.5",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@biomejs/biome": "2.5.2",
    "@tailwindcss/vite": "^4.3.2",
    "axe-core": "^4.11.0",
    "lefthook": "^2.1.9",
    "puppeteer-core": "^25.3.0",
    "tailwindcss": "^4.3.2",
    "typescript": "^6.0.3",
    "wrangler": "^4.106.0"
  }
}
